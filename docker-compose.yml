version: '3.9'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ventureledger
      POSTGRES_USER: ventureledger
      POSTGRES_PASSWORD: ventureledger
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
